{
  "name": "valorant-hub-server",
  "version": "1.0.0",
  "description": "AI-powered backend for Valorant HUB with OpenAI function calling",
  "main": "dist/index.js",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "test": "ts-node-dev --respawn --transpile-only src/test-server.ts",
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "openai": "^4.20.1",
    "dotenv": "^16.3.1",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "typescript": "^5.2.2",
    "@types/express": "^4.17.21",
    "@types/node": "^20.8.7",
    "@types/cors": "^2.8.15",
    "ts-node-dev": "^2.0.0"
  }
}
